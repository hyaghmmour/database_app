import 'package:flutter/material.dart';

class GradientWidget extends StatelessWidget {
  GradientWidget(List<Colors> colors);
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
